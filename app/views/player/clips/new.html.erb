<br />
<div class="booyah-box col-10 offset-1">
  <h1>Add a new clip</h1>
   <%= simple_form_for @clip, url: player_clips_path do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <br />
    <%= f.submit "Submit", class: 'btn btn-primary' %>
    <br />
  <% end %>
  <br />
</div>